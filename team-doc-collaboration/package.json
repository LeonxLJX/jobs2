{
  "name": "team-doc-collaboration",
  "version": "1.0.0",
  "private": true,
  "description": "Multi-tenant team document collaboration platform (root orchestration)",
  "scripts": {
    "install:all": "npm --prefix backend install && npm --prefix frontend install",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "dev": "concurrently \"npm:dev:backend\" \"npm:dev:frontend\"",
    "build:backend": "npm --prefix backend run build",
    "build:frontend": "npm --prefix frontend run build",
    "build": "npm run build:backend && npm run build:frontend",
    "setup": "npm --prefix backend install && npm --prefix backend run db:generate && npm --prefix backend run db:push && npm --prefix backend run seed && npm --prefix frontend install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
